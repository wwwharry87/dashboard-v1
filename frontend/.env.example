# Variáveis do frontend (React)
# Em produção (Render Static Site), adicione estas em Settings > Environment
# cp .env.example .env  (para rodar local)

# URL pública do backend (com /api no final)
REACT_APP_API_URL=https://SEU-BACKEND.onrender.com/api
